{
  "name": "photoshop",
  "private": true,
  "version": "0.1.0",
  "description": "Garau Transparency — Photoshop UXP plugin. A thin client of @garau/engine.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@garau/engine": "*"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0"
  }
}
